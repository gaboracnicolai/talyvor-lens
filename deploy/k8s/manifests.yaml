# GENERATED by `make k8s-manifests` from deploy/helm/lens — Helm is the source of
# truth; do not edit by hand. Regenerate after any chart change.
---
# Source: lens/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lens
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor

---
# Source: lens/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: lens-config
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
data:
  # Non-secret gateway configuration. Connection strings + API keys live in the
  # Secret (see secret.yaml / secret.existingSecret), NOT here.
  LENS_LISTEN_ADDR: "0.0.0.0:8080"
  LENS_LOG_LEVEL: "info"
  LENS_HA_ENABLED: "false"
  LENS_HA_HEARTBEAT_SEC: "5"
  LENS_HA_INSTANCE_TTL_SEC: "15"
  LENS_HA_DRAIN_TIMEOUT_SEC: "30"
  # Billing (U18b) — FIAT. Stripe SECRETS live in the Secret, never in this ConfigMap.
  LENS_BILLING_ENABLED: "false"
  LENS_BILLING_SUCCESS_URL: "https://app.talyvor.com/billing/success?session_id={CHECKOUT_SESSION_ID}"
  LENS_BILLING_CANCEL_URL: "https://app.talyvor.com/billing/cancel"

---
# Source: lens/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: lens
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

---
# Source: lens/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lens
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
    app.kubernetes.io/component: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: lens
      app.kubernetes.io/instance: lens
      app.kubernetes.io/component: gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lens
        app.kubernetes.io/instance: lens
        app.kubernetes.io/component: gateway
      annotations:
        # Roll pods automatically when the non-secret config changes.
        checksum/config: 11a6af67b38f29a998fc52a839324b8862ae58443bba8538ae06fd64189e1a0d
    spec:
      serviceAccountName: lens
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: lens
          image: "ghcr.io/gaboracnicolai/talyvor-lens:latest"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            # Non-secret config.
            - configMapRef:
                name: lens-config
            # Sensitive config (DSNs + API keys). See secret.existingSecret.
            - secretRef:
                name: lens-env
          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          # Read-only root FS (securityContext) needs a writable temp dir.
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
# Source: lens/templates/job-migrate.yaml
# Database migration hook. Runs as a pre-install AND pre-upgrade hook so the
# schema is migrated BEFORE new gateway pods roll out (ordering: this Job must
# complete successfully before the Deployment is created/updated). If the Job
# fails (non-zero exit), Helm fails the install/upgrade — so a server is never
# rolled out against an unmigrated DB.
#
# The default command is the gateway image's own `lens migrate` subcommand,
# which applies the embedded SQL migrations idempotently (tracked in a
# schema_migrations table) using the same LENS_DATABASE_URL the server uses.
# Override migrations.command to use a different mechanism, or set
# migrations.enabled=false to skip the hook entirely and run migrations
# externally.
apiVersion: batch/v1
kind: Job
metadata:
  name: lens-migrate
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
    app.kubernetes.io/component: migrate
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lens
        app.kubernetes.io/instance: lens
        app.kubernetes.io/component: migrate
    spec:
      restartPolicy: Never
      serviceAccountName: lens
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: "ghcr.io/gaboracnicolai/talyvor-lens:latest"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          command:
            # The gateway image's built-in migrator. Applies embedded SQL
            # idempotently; exits non-zero on failure so this hook Job fails
            # and Helm holds back the rollout.
            - /lens
            - migrate
          envFrom:
            - configMapRef:
                name: lens-config
            - secretRef:
                name: lens-env
          resources:
            {}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}

