# GENERATED by `make k8s-manifests` from deploy/helm/lens — Helm is the source of
# truth; do not edit by hand. Regenerate after any chart change.
---
# Source: lens/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lens
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor

---
# Source: lens/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: lens-config
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
data:
  # Non-secret gateway configuration. Connection strings + API keys live in the
  # Secret (see secret.yaml / secret.existingSecret), NOT here.
  LENS_LISTEN_ADDR: "0.0.0.0:8080"
  LENS_LOG_LEVEL: "info"
  LENS_HA_ENABLED: "false"
  LENS_HA_HEARTBEAT_SEC: "5"
  LENS_HA_INSTANCE_TTL_SEC: "15"
  LENS_HA_DRAIN_TIMEOUT_SEC: "30"

---
# Source: lens/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: lens
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

---
# Source: lens/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lens
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
    app.kubernetes.io/component: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: lens
      app.kubernetes.io/instance: lens
      app.kubernetes.io/component: gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lens
        app.kubernetes.io/instance: lens
        app.kubernetes.io/component: gateway
      annotations:
        # Roll pods automatically when the non-secret config changes.
        checksum/config: 2af8dbc12f8a7bf875c43503c8b4ae05e5d0ecb9058c937313f7b660e950c54a
    spec:
      serviceAccountName: lens
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: lens
          image: "ghcr.io/talyvor/talyvor-lens:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            # Non-secret config.
            - configMapRef:
                name: lens-config
            # Sensitive config (DSNs + API keys). See secret.existingSecret.
            - secretRef:
                name: lens-env
          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          # Read-only root FS (securityContext) needs a writable temp dir.
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
# Source: lens/templates/job-migrate.yaml
# Database migration hook. Runs as a pre-install AND pre-upgrade hook so the
# schema is migrated BEFORE new gateway pods roll out (ordering: this Job must
# complete successfully before the Deployment is created/updated).
#
# IMPORTANT: the default Lens gateway image ships neither a `migrate`
# subcommand nor the SQL files, so there is no working default command. You
# MUST set migrations.command to your migration mechanism (a dedicated
# migrations image, a `migrate`/flyway tool, etc.) — see the chart README
# "Migrations". With no command set, this Job intentionally fails closed rather
# than pretend migrations ran. Set migrations.enabled=false to skip entirely.
apiVersion: batch/v1
kind: Job
metadata:
  name: lens-migrate
  labels:
    helm.sh/chart: lens-0.1.0
    app.kubernetes.io/name: lens
    app.kubernetes.io/instance: lens
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: talyvor
    app.kubernetes.io/component: migrate
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lens
        app.kubernetes.io/instance: lens
        app.kubernetes.io/component: migrate
    spec:
      restartPolicy: Never
      serviceAccountName: lens
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: "ghcr.io/talyvor/talyvor-lens:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          command:
            - /bin/sh
            - -c
            - >-
              echo 'ERROR: migrations.command is not set. Lens ships no migrate
              subcommand and the SQL is not in the gateway image, so there is no
              safe default. Configure migrations.command (see chart README →
              Migrations) or set migrations.enabled=false.' >&2; exit 1
          envFrom:
            - configMapRef:
                name: lens-config
            - secretRef:
                name: lens-env
          resources:
            {}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}

